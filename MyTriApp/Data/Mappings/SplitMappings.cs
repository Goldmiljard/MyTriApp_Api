using Microsoft.EntityFrameworkCore.Metadata.Builders;
using Microsoft.EntityFrameworkCore;
using MyTriApp.Data.Entities;

namespace MyTriApp.Data.Mappings
{
    //public class SplitMappings : IEntityTypeConfiguration<Split>
    //{
    //    public void Configure(EntityTypeBuilder<Split> builder)
    //    {
    //        builder.HasKey(s => s.Id);
    //        builder.HasOne(s => s.Activity).WithMany(a => a.Splits);
    //    }
    //}
}
