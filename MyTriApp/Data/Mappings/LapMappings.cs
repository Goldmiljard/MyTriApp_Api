using Microsoft.EntityFrameworkCore.Metadata.Builders;
using Microsoft.EntityFrameworkCore;
using MyTriApp.Data.Entities;

namespace MyTriApp.Data.Mappings
{
    //public class LapMappings : IEntityTypeConfiguration<Lap>
    //{
    //    public void Configure(EntityTypeBuilder<Lap> builder)
    //    {
    //        builder.HasKey(s => s.Id);
    //        builder.HasOne(s => s.Activity).WithMany(a => a.Laps);
    //    }
    //}
}
